server:
  port: 8080
spring:
  application:
    name: register

eureka:
  client:
    register-with-eureka: false # 不注册自己
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8080/eureka/